#include<stdio.h>

int main()
{
	int num,i,j,sum=0;
	printf(" \n enter the term");
	scanf("%d",&num);
	for( i= 1;i<num+1;i++)
	{
		if(i%2!=0)
		{
			sum=sum+(i*i);
		}
	}
	printf(" \n the sum of square of number till %d no. is : %d", num,sum);
}
